/**
 * main-contact.js
 * Entry point untuk contact.html (halaman Kontak).
 */

import './styles/main.css';
import { initContactPage } from './controllers/contact.controller.js';

document.addEventListener('DOMContentLoaded', initContactPage);